---
name: checks

on: [push]

jobs:
  test:
    name: Testing
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Install Pytest
        run: pip install pytest
      - name: Test
        run: pytest